#include<stdio.h>
#include<conio.h>
void main()
{
int i,n;
clrscr);
long long int fact=1;
printf("Enter a number\n");
scanf("%d",&n);
for(i=0;i<n;i++){
fact=fact*i;
}
printf("Factorial of the given number is %d\n",fact);
getch();
}
